<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Destinations</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="shortcut icon" href="#" type="image/x-icon">
</head>
<body>
    <!-- 
        <% if (locals.destinations) { %> 
        <% for (let i = 0; i < locals.destinations.length; i++) { %> 
            <a href="/destinations/<%=locals.destinations[i].id%>">
                <div>
                    <span>ID: <%= locals.destinations[i].id %></span>
                    <span>Country: <%= locals.destinations[i].country %></span>
                    <span>City: <%= locals.destinations[i].city %></span>
                    <span>Budget: <%= locals.destinations[i].budget %></span>
                </div>
            </a>
        <% } %> 
    <% } %>

    <form action="/logout" method="post">
        <button type="submit">Logout</button>
    </form>
    -->
    <div class="navbar">
        <div class="container">
            <h1>GroupTrip<span>Planner</span></h1>
            <!-- TODO LATER: Mobile and Desktkop Nav-->
        </div>
    </div>
    <section class="destinations">
        <div class="container">
            <div class="content-header row">
                <span class="number">#</span>
                <span class="city">City</span>
                <span class="country">Country</span>
                <span class="budget">Budget</span>
                <span class="nights-stay">Stay</span>
            </div>
            <div id="destination-list" class="list">
                <% if (locals.destinations) { %> 
                    <% for (let i = 0; i < locals.destinations.length; i++) { %>
                            <div id="<%= locals.destinations[i].id %>" class="list-item row" onclick="viewDestination(this.id)">
                                <span class="number"><%= i + 1 %> </span>
                                <span class="city"><%= locals.destinations[i].city %></span>
                                <span class="country"><%= locals.destinations[i].country %></span>
                                <span class="budget">&#x20AC;<%= locals.destinations[i].budget %></span>
                                <span class="nights-stay"><%= locals.destinations[i].nights_stay %> nights</span>
                            </div>
                        </a>
                    <% } %> 
                <% } %>
            </div>
        </div>
    </section>
    <script src="/js/destinations.js"></script>
</body>
</html>