<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Destinations</title>
</head>
<body>
    <% if (locals.destinations) { %> 
        <% for (let i = 0; i < locals.destinations.length; i++) { %> 
            <a href="/destinations/<%=locals.destinations[i].id%>">
                <div>
                    <span>ID: <%= locals.destinations[i].id %></span>
                    <span>Country: <%= locals.destinations[i].country %></span>
                    <span>City: <%= locals.destinations[i].city %></span>
                    <span>Budget: <%= locals.destinations[i].budget %></span>
                </div>
            </a>
        <% } %> 
    <% } %> 
</body>
</html>